import { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Dashboard',
    description: 'Descripcion dashboard'
}

export default function Page() {
    return <p>Dashboard Page</p>;
  }