import { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Invoices',
    description: 'Descripcion invoices'
}

export default function Page() {
    return <p>Invoices Page</p>;
  }